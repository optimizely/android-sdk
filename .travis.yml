language: java
jdk:
  - openjdk7
  - oraclejdk7
  - oraclejdk8
install: true
script:
    - "./gradlew clean build check"
cache:
  gradle: true
  directories:
  - "$HOME/.gradle/caches"
  - "$HOME/.gradle/wrapper"
branches:
  only:
  - master
  - devel
